# Local Windows/macOS loop — prefer wheels; skip GCS (use BACKGROUND_IMAGE_PATH file).
# Docker / Cloud Run still use requirements.txt.
fastapi==0.115.6
uvicorn==0.32.1
google-genai>=1.10.0
pillow>=11.1.0
python-multipart==0.0.20
