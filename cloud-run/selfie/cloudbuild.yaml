steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-f'
      - 'cloud-run/selfie/Dockerfile'
      - '-t'
      - '${_IMAGE_NAME}'
      - '.'
images:
  - '${_IMAGE_NAME}'
