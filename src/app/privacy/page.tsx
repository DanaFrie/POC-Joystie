'use client';

import { OnboardingPrivacyScreen } from '@/components/legal/OnboardingPrivacyScreen';

/** `/privacy` — privacy policy (top-level). */
export default function PrivacyPage() {
  return <OnboardingPrivacyScreen />;
}
