'use client';

import { OnboardingTermsScreen } from '@/components/legal/OnboardingTermsScreen';

/** `/terms` — terms of use (top-level, not under onboarding). */
export default function TermsPage() {
  return <OnboardingTermsScreen />;
}
